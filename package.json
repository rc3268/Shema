{
  "name": "shema",
  "version": "1.0.0",
  "private": true,
  "description": "Native store wrapper for the Shema sermon-notes PWA (Capacitor). The app itself remains a build-step-free static site in index.html/sw.js/manifest.json -- this only exists to hand that same site to iOS/Android as a native shell.",
  "scripts": {
    "build": "node scripts/build-www.js",
    "gen-icons": "python3 scripts/gen-native-icons.py",
    "build:vendor-purchases": "esbuild scripts/vendor-src/purchases-entry.js --bundle --format=iife --outfile=vendor/purchases-capacitor.js",
    "sync": "npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/cli": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2",
    "@revenuecat/purchases-capacitor": "^13.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
