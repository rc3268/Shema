# Context for Shema

This file exists to bring a new Claude chat or Cowork session up to speed on this project without re-deriving decisions that have already been made. Read this before making product, design, or architecture calls here — it captures the *why*, not just the *what* (the *what* is in `README.md` and the code itself).

## Agenda: next session

**Review every change made after 7PM on 2026-07-22** before starting any new work -- the user asked for this explicitly before logging off, right after merging the PR covering that day's work. Walk the commits/diff from that point forward (conflict false-positive fix, sign-in-persistence mitigation, Practice note excerpt fix, Library year filter, Plan search) with the user rather than just re-reading History and assuming it's fine. Delete this section once that review actually happens.

## End goal

Ship Shema as a **paid native app on the Apple App Store and Google Play Store**. Target user: pastors and teachers who prepare a sermon on a phone or tablet and want that same sermon available on a computer too — cross-device continuity is a core requirement, not a nice-to-have, but (see Business model) computer access is specifically a sync-subscriber benefit, not something the free web ever provides.

## Business model

Revised 2026-07-21 after a full pricing/distribution pass -- this replaces an earlier draft that had the local app itself free-forever-no-account with an optional sync add-on. That framing got reconsidered from first principles, not just tweaked; the reasoning is worth keeping, not just the conclusion:

- **The native app is the product, sold as a genuine paid store listing (~$19.99), not an in-app unlock.** No trial, no feature gate, no "free" download that then asks for money -- what the store page shows is exactly what you get, day one, in full. Two reasons this beats a free-download-with-IAP-trial model for this specific market: (1) pastors are a slow, trust-driven, often committee-gated buyer who mostly arrive via referral/demo/word-of-mouth, not cold App Store browsing -- so "free removes browse friction" matters far less here than it would for a browse-and-impulse-install category; (2) utility apps that gate real functionality behind an undisclosed-until-you-hit-it trial draw a disproportionate, permanent share of "you have to pay to use this?!" one-star reviews, which is a much sharper risk for a brand-new app with few reviews to dilute them than for an established one. A paid listing pre-filters that entirely -- nobody who wasn't willing to pay ever installs it.
- **There is no standalone free web app for the general public.** This is the one deliberate reversal from the original plan. Since a store-listing purchase happens entirely inside Apple's/Google's own receipt systems, a website has no way to ever verify "this visitor legitimately bought the native app" -- so the web build can only ever gate on one thing: a signed-in account with an active sync subscription. Given that constraint, letting the web build stay a fully-functional, no-login, no-payment alternative would just cannibalize the native app's whole value proposition for free. So the public web URL becomes a marketing/landing page instead (what the app does, screenshots, App Store/Play Store links) with a sign-in box for existing sync subscribers to reach their synced content -- not a working local-only editor for anyone who shows up.
- **The monthly/yearly sync subscription is the only way Shema is ever usable on a computer.** Not "cross-device sync as a nice-to-have on top of a phone app" -- it's the entire justification for paying recurring money at all. A local-only native app purchase is phone/tablet-only, permanently, by design; wanting it on a computer too is specifically what the subscription is for. This also resolved a pricing-ratio worry from the same discussion: sync's price no longer needs to be judged against the one-time unlock price at all, since the two aren't comparable purchases anymore (one's a phone app, the other is what makes it a desktop tool too).
- **Technical consequence, not yet applied to the code**: since the native purchase is a plain store listing, it needs no RevenueCat/webhook entitlement tracking at all -- Apple/Google enforce it natively, with zero custom code. Only the `sync` entitlement still needs the RevenueCat client SDK + the Supabase webhook (already built, see the Phase 4 History entries) -- the `owns_app` entitlement, the local-device RevenueCat check, and the "Unlock the full app" purchase button added to the Account screen during that same pass are now dead weight from the superseded plan and need removing, not just leaving inert.
- No Apple Developer account exists yet (as of this writing). Packaging/store submission work is intentionally deferred until the app itself is in a good place — see "Roadmap" below.

**Pricing, finalized this pass**: **one-time app purchase $19.99** (down from an earlier $29.99–39.99 draft range -- deliberately anchored as an easy, low-friction yes rather than a premium price, since the whole point of a paid-up-front listing is removing hesitation, not maximizing per-unit revenue), **sync subscription unchanged at $4.99/mo or $39.99/yr** (its value story changed, per above, but its cost structure -- real recurring Supabase/storage cost, unlike the zero-marginal-cost local purchase -- didn't, so there was no principled reason to move it).

## Design principles

- **Offline-first, always.** A pastor walking into a venue with no signal must have a fully working app. Sync (once built) reconciles in the background and never blocks Prepare or Present.
- **"Simple, graceful" visual theme.** Recent UI work has consistently traded density for calm — e.g. the Prepare-screen redesign moved the sermon title out of the main view into a popup (reopenable by tapping the title in the top bar) and collapsed the formatting toolbar into a toggleable sidebar, specifically because the prior layout felt "crowded" and off-theme. Default to this instinct: when adding a feature, ask whether it can live behind a toggle/popup rather than adding permanent on-screen chrome.
- **Present mode is for actually preaching from, not just displaying text.** Large type, a stage countdown timer, minimal chrome. This view's needs (can the presenter tell there's more content below the fold without looking away from the audience?) are treated as a distinct design problem from the editor's needs — decisions that are fine in Prepare (e.g. hiding a scrollbar for cleanliness) are evaluated separately for Present, where the same choice has a live-presentation-risk dimension.
- **Complementary to ProPresenter, not competing with it.** Shema exports to ProPresenter rather than trying to replace a church's existing projection/worship-presentation software. This is a deliberate go-to-market wedge ("the sermon-writing companion to the presentation software you already run"), not an accident of scope.
- **Single-file app, kept diffable.** `index.html` holds all markup/CSS/JS by design (no build step, trivial to serve/test). To keep it reviewable as it grows: icons live in `icons/` as standalone files (not inlined base64) with a `data-light`/`data-dark` attribute convention for theme variants, and fonts are self-hosted as base64 `@font-face` rules rather than pulled from a CDN, so the app has zero runtime network dependency for its own UI.

## Current architecture snapshot

- Plain PWA: `index.html` + `sw.js` (service worker, versioned `CACHE_NAME`) + `manifest.json`. No backend, no accounts. As of Phase 4 (see History, 2026-07-21), an `ios/`/`android/` Capacitor wrapper also exists purely to hand the same static files to the two app stores -- `npm run build` copies them into `www/` (gitignored, regenerate with `scripts/build-www.js`) unmodified; the PWA itself has no build step and never will.
- Data lives in `localStorage` only (see `README.md` for the exact keys). No server round-trip exists yet.
- As of the "Phase 1" work (see History), the data model is deliberately **sync-ready but not yet synced**: sermons and illustrations use UUIDv4 ids, carry `updatedAt`/`schemaVersion`, and deletes are tombstones (`deleted:true`) rather than array removal, specifically so a future sync engine can propagate a delete instead of resurrecting it. Backup export/import already does real last-write-wins merge by id+`updatedAt` as a preview of that logic on a single device.
- Responsive design has explicit breakpoints (480/768/1000/1600px) and, as of the most recent change, wide-screen content columns use **fixed pixel margins from the screen edge** rather than a centered `max-width` cap — content keeps growing on ultra-wide monitors instead of floating as a narrow centered island.
- Practice Mode (see History, 2026-07-20) introduced the app's first use of **IndexedDB** (`shema_practice` db) alongside `localStorage` -- audio recordings are real Blobs, too large and the wrong shape for `localStorage`'s string-only, ~5-10MB-ceiling model. Kept intentionally separate from every other `persist*`/`saveJSON` local-storage path and from the Supabase sync engine: practice audio never syncs, never leaves the device, by design.

## Competitive positioning (researched mid-2026, re-verify before acting on pricing)

The direct "sermon-outlining app" niche is thin. The closest real competitor is **Sermonary** (web-only, $49–99/mo, AI-writing-first, no confirmed ProPresenter integration) — priced as a premium outlier. **Preach: Sermon Builder** is the closest small native-app peer (free w/IAP, modest but real traction). Everything else is adjacent, not competing head-on: **Proclaim** and **ProPresenter** are whole-team worship-production software priced per seat ($18–72/mo, $29–59/mo); **Logos** bundles a sermon builder into its $9.99–19.99/mo study-suite subscription rather than selling it alone; **Planning Center** is team scheduling/ops, priced by headcount.

This is real whitespace, but cuts both ways: nobody has made a big hit out of "just sermon writing + present mode," which could mean it's unexecuted opportunity, or could mean pastors don't want to pay separately for a point solution when a bundled tool (Logos, Proclaim) already covers something similar. Treat this as an open risk, not a solved question — validate with real users before over-investing in features nobody asked to pay for separately.

Biggest go-to-market barriers identified (see full analysis from the market-evaluation session): near-zero organic App Store discoverability against incumbents with existing installed bases; pastors as a slow, trust-driven, often committee-gated buyer; no track record yet (cold start); growth partly tethered to ProPresenter's own visibility given the export-hook strategy; feature-race risk against a funded competitor (Sermonary) if differentiation drifts away from simplicity/offline-reliability/price.

## Roadmap

Phased plan to get from the current local-only PWA to synced, store-ready apps. Each phase is described in more depth in the conversation history that produced this file; summarized here for continuity.

1. **Data model sync-readiness — ✅ done.** UUIDv4 ids, tombstone deletes, per-record `schemaVersion`, persisted `deviceId`, last-write-wins backup merge. See History below for the commit.
2. **Accounts & entitlement — ✅ auth done, entitlement-writing intentionally deferred to Phase 4.** Backend decision made: Supabase. Google OAuth and email (magic link) sign-in are live, wired into the app, and confirmed with a real human click-through (not just a mocked test) — signed in successfully as a real Google account, landed back in the app already authenticated, entitlement display correctly read "Not yet" / "Not active". Sign in with Apple still deferred until an Apple Developer account exists (remember: Apple requires it the moment any other third-party login is offered, so it must land before App Store submission, not before then). `profiles` table (`owns_app`, `sync_subscription_active`, one row per `auth.users` id, auto-created via trigger on signup) is live with RLS locked to read-only for the client — entitlement flags are deliberately never client-writable, since an UPDATE policy on those columns would let a signed-in user grant themselves the paid unlock for free; they'll only ever be set by a server-side purchase-verification webhook (Phase 4, via the service_role key, which bypasses RLS) — that webhook, not this phase, is what will actually flip these flags to true. App side: `vendor/supabase.js` (self-hosted, precached, not CDN-loaded), a full Account management screen reachable from a new Settings screen (see below) with sign-in/out, an account-type badge, entitlement status, and the typical placeholder buttons a subscription screen needs (Manage Subscription, Restore Purchases, billing date/amount) ahead of Phase 4 actually wiring them up.

Also introduced a **Settings screen** (gear icon, replacing the old person-icon Account button in the brandbar) as the home for app-wide, infrequent actions -- Account, Backup, Restore from Backup, and Dark Mode all live there now, decluttering the Library header down to just "+ New". Import stays in Prepare, deliberately not moved here: it's a per-sermon authoring action tied to the editor, not an app-wide setting, and a `local` user needs it exactly as much as a `sync` user does.

**Account types** (client-computed, not a new schema column): `local` (no session -- the permanent default), `dev` (signed in as one specific hardcoded email -- a display label only, not a real permission boundary, since there's nothing sensitive gated behind it yet), `sync` (signed in with `sync_subscription_active` true -- nobody yet, since nobody can pay until Phase 4), `tester` (everyone else who's signed in -- which, practically, is *everyone* right now, since paying isn't possible yet; "tester" is simply what a signed-in, non-dev, non-paying account means during this pre-launch window).
3. **Sync engine — 🚧 in progress, sermons done, illustrations/templates next.** Decided the open conflict question from the original roadmap note: not silent LWW. The same record edited on two devices between syncs never silently drops an edit -- the remote version becomes canonical under the original id, and the local version is preserved as a new record (title suffixed "(synced copy)", `conflictOf` pointing at the original), surfaced with a small tag on its card. Reasoning: sync is invisible/automatic by design (that's the point of paying for it), so nobody is watching a merge happen the way a manual backup-restore user is -- a silent loss would go unnoticed, and a blocking "resolve this now" prompt could land at the worst possible moment (a pastor about to walk on stage). Deferring the decision to whenever the user happens to open the app, with no forced UI, fit better than either alternative. Increasing sync frequency was considered as a mitigation (shrinks the window where two online devices diverge) but doesn't touch the offline-divergence case at all, so it's a complement, not a fix -- not built as a separate lever yet (sync currently fires on every local save, debounced 2.5s, plus every 60s and on regaining connectivity, which is already fairly aggressive).
   - New Supabase tables, RLS same shape as `profiles` but full owner CRUD (not read-only) since this is the user's own content, not an entitlement flag: `sermons`, `illustrations` (with an `image_path` column pointing at a new private `illustration-images` Storage bucket rather than storing base64 in a column), `templates`.
   - Client-side sync engine (in `index.html`, right before INIT): per-record last-synced timestamps in a new `shema_sync_meta_v1` key, a generic `syncOneTable(cfg)` pull-then-push pass (currently wired for `sermons` only), triggered by `persistSermons()` (debounced 2.5s via `scheduleSync()`), a 60s interval, the `online` event, and auth state changes. Local storage stays authoritative always -- sync only ever mirrors it outward/inward, never blocks a local read/write, and runs for any signed-in user (not gated on `sync`-type entitlement, since nobody can pay yet).
   - Verified with a mocked-Supabase Playwright script covering all three cases: push a new local record, pull a remote edit, and the true-conflict case (edit the same record on both "sides" between syncs) -- confirmed the conflict path creates the tagged copy instead of dropping either edit.
   - Illustrations and templates now sync the same way. Illustration images ride as a Storage object rather than a jsonb/text column: `imagePath` is a lightweight local cache hint (which Storage path this device has already uploaded/downloaded) separate from `imageData` (the actual base64, always the offline-first source of truth, never cleared by a pull) so unrelated pushes don't re-upload unchanged image bytes. Fixed a real bug this surfaced: template deletion was array-removal (`templates.filter(...)`), not a tombstone like sermons/illustrations -- under sync that would have made a deleted template silently reappear (remote still has the row, this device just "hasn't seen it," so a pull would re-add it). Switched it to `deleted:true` + `renderTemplateList()` filtering it out, and updated "Restore built-in templates" to un-delete a tombstoned built-in in place instead of assuming the id is gone.
   - The `templates` table needs one more column the original migration missed: `desc` (templates carry a `name` *and* a `desc`, and the first migration only added `name`). Run `alter table public.templates add column if not exists "desc" text;` in the Supabase SQL editor before this reaches a device with existing templates to sync.
   - Verified with a second mocked-Supabase Playwright pass: illustration push uploads the image to the bucket and stamps `image_path` on the row, template push preserves `desc`, and pulling a remote-only illustration downloads and reconstitutes its image into local `imageData`.
   - Fixed a real, longstanding, sync-unrelated bug found while testing this live: inserting an illustration into a sermon (the picker) only ever copied its text onto the sermon item, never its image -- and no renderer (editor item list, Present's stage, manuscript/RTF export) had any code to display an image inline in sermon content at all, so a pictured illustration's image was silently dropped the moment it was used inside a sermon. Now the sermon item carries `imageData` too, with a small thumbnail in the editor's item row and a larger image above the text in Present. Manuscript/RTF export still don't show it -- lower priority, not requested, revisit if it comes up. Worth remembering: sermon `sections` sync as one jsonb blob per sermon, so an image used inside a sermon now rides along inside that column rather than through the illustration-images Storage bucket -- the same base64-bloat consideration flagged for illustrations applies here too and isn't solved yet.
   - Also surfaced a real gap during live two-device testing: illustrations pushed from one device weren't reliably reaching the other device's Illustrations library (separate from the sermon-item image issue above -- the illustration record itself, standalone in the library, didn't sync). Every sync error was previously swallowed silently by design (best-effort, never spam the user with sync noise) -- added `console.error` logging at each failure point (select, upsert, image upload, image download, and the outer per-pass catch) so a real failure is at least visible in devtools instead of invisible. Root cause not yet confirmed -- likely candidates: a device still running the pre-illustration-sync service worker version (very plausible, since that code landed one commit after sermon sync did and this app requires an actual reload to pick up a new SW version), or a real Storage/RLS issue on the illustration-images bucket specifically (the one path that isn't plain Postgres RLS, so it's the one thing the earlier mocked tests structurally couldn't validate). Needs a live retry with both devices confirmed on the latest deploy before concluding which.
   - Root cause found via the new logging: some local records (from before `crypto.randomUUID` was consistently available -- it needs a secure context, so early testing over plain HTTP or an older browser fell back to `uid()`'s non-crypto path) never actually had real UUIDs for ids, despite Phase 1's roadmap note claiming "UUIDv4 ids." The `sermons`/`illustrations`/`templates` tables were created with a strict `uuid` primary key, so Postgres rejected every push of one of these legacy-id records outright (`22P02 invalid input syntax for type uuid`) -- silently, once error logging was added it showed clearly. Fixed at the schema level, not the client: `id` (and the self-referencing `conflict_of` / `source_illust_id` columns) changed from `uuid` to `text` on all three tables, since the app already treats ids as opaque strings everywhere and never needed real UUID typing at the database layer. This was very likely also the illustration-not-syncing bug from the previous entry -- same failure mode, same fix.
   - Added the resolution step the earlier design note said would come "whenever the user gets to it": every conflict-copy card (sermon, illustration, template) now has two extra actions alongside the existing Edit/Delete -- "Keep This" (overwrite the original with this version's content and retire this copy) and "Keep Both" (clear the conflict flag, strip the "(synced copy)" title suffix, and let both stand permanently as separate records). The existing Delete button already covered the third case (discard this copy, leave the original untouched), so no new delete-specific logic was needed. "Keep This" only appears if the original record can still be found (not itself deleted); otherwise only "Keep Both" shows, since there's nothing left to replace. Templates track `conflictOf` locally for this same UI, but it isn't pushed to Postgres (the `templates` table has no `conflict_of` column) -- low priority to add given how rarely a template would actually be edited concurrently on two devices; can revisit if it turns out to matter.
   - Confirmed live by the user on real devices: the `uuid`→`text` id-column migration cleared the stuck sermons/illustration, and the Present-mode illustration-image fix both work correctly across two real devices. Phase 3's core sync engine is functionally done; remaining known gaps are the two lower-priority items noted above (sermon-embedded image storage-bloat, template `conflict_of` not in Postgres), not required before moving on.
   - Settings and Account, previously bottom sheets like every other modal in the app, are now a right-side drawer (full height, slides in over a dimmed backdrop, dismissible by tapping the backdrop) -- kept as an additive `.drawer-right` modifier class so no other modal's behavior changed. Moved the gear icon to the right edge of the brand bar to match (it's `margin-left:auto` now instead of just trailing the wordmark) since a right-aligned trigger reads more naturally as "opens a right-side panel" than a left-aligned one did. Also gave Version History and Import Notes the same drawer treatment (Import keeps a wider 640px cap, override via an inline style since it needs its toolbar+editor room) -- picked those two specifically because both are a "sit and work/read for a bit" experience like Settings/Account, unlike the quick-pick/quick-form modals (template picker, sermon info, illustration add/insert) that stayed as bottom sheets on purpose.
   - Closed out both bloat/visibility gaps flagged above. Sermon-embedded illustration images now follow the exact same Storage-object pattern as the illustrations table (`sermonBeforePush`/`sermonAfterPull`), keyed by the sermon item's own id under `${userId}/sermon-items/${itemId}.jpg` (not the source illustration's id, since the same illustration can be inserted into several items/sermons, each needing an independent copy that survives the original being edited or deleted) -- `sections` sent to Postgres now always has `imageData` stripped per item, `imagePath` is the only thing that rides in the jsonb column. And sync is no longer a black box: `lastSyncInfo` tracks the last pass's outcome, surfaced in the library header (replacing, for a signed-in user only, what used to be the manual-backup nag -- see next bullet) and re-rendered on every pass start/finish so "Syncing…" / "Synced Xm ago" / "Sync error · Xm ago" is always current. A "Sync Now" row appeared in Settings (visible only when signed in) wired to a `manualSyncNow()` that skips the normal 2.5s debounce but is still hard rate-limited to once per 5 seconds.
   - Decided the "Last backup" library-header question explicitly: a signed-in user (sync running) now sees sync status there instead of the manual-backup nag, since showing "Never backed up"/stale-backup warnings to someone who's actually fully protected by continuous cloud sync was actively misleading, not just redundant. The Settings screen's own "Restore from Backup" row is untouched and still always shows the manual-export status regardless of sync state -- a portable JSON copy is still worth having even when synced, so that meaning wasn't overloaded.
4. **Store packaging — 🚧 in progress.** Capacitor recommended for wrapping the PWA for both iOS and Android from one codebase (a Trusted Web Activity would only cover Android). RevenueCat recommended to unify StoreKit + Play Billing behind one API and offload receipt/subscription validation, rather than building that verification backend by hand. Ordered plan, laid out 2026-07-21:
   - **Open tension, decided**: adding Capacitor means adding a real build step (`npm`/`npx cap`, native Xcode/Android Studio projects) to an app whose founding design principle is "single-file, no build step, trivial to serve/test." Confirmed deliberately, not defaulted into -- the web/PWA version (GitHub Pages, installable PWA) is untouched and still needs nothing but the repo's static files; Capacitor's `webDir` is those same files copied verbatim by `scripts/build-www.js`, never transformed.
   - **What can be built and tested for $0, in order:**
     1. ✅ Capacitor scaffold done: bundle id `com.shema.app`, both `ios`/`android` platforms added. Not yet done: an actual device/simulator run to confirm the CSP's `'self'`-scoped `connect-src`/`img-src` resolves correctly against Capacitor's real WebView origin -- reasoned through as likely fine (same-origin bundled assets), but unverified without Xcode/Android Studio.
     2. ✅ Native app icons + splash done, generated from `shema-icon-512.png`/`shema-mark-color.png` (see History) -- caveat: source is only 512px, a true 1024px+ master would look sharper before final submission.
     3. ✅ Microphone usage descriptions done: `NSMicrophoneUsageDescription` (iOS) and `RECORD_AUDIO` (Android), for Practice Mode's existing `getUserMedia` call.
     4. ✅ RevenueCat client SDK + the purchase-verification webhook built and fully unit-tested, sync-only now (see History for the cleanup pass that dropped the superseded `owns_app` half).
     5. ✅ RevenueCat project now exists and is live end-to-end for Android (see History, 2026-07-21 entries): `sync` entitlement, a `sync` Offering with `sync-monthly`/`sync-yearly` packages, a real public API key wired into `index.html`'s `REVENUECAT_API_KEY_ANDROID`, and the webhook deployed to Supabase with its secrets set and verified via RevenueCat's own "Send test event" (real 200 response). The iOS half of the RevenueCat project itself is still blocked on the Apple Developer Program membership -- App Store Connect's In-App Purchase key is a hard prerequisite RevenueCat's iOS app setup can't get past.
     6. iOS: a local StoreKit Configuration file in Xcode simulates the sync subscription's lifecycle with zero paid account -- **this does not need the Apple Developer Program**, only Xcode + a free Apple ID + the Simulator; the paid membership only gates App Store Connect itself (the IAP key, TestFlight, real submission), not local Simulator testing. Not yet attempted, needs the user's own Mac. Android: Play Console's license-tester and reserved test product ID tools cover the equivalent free testing, but actually do require the $25 one-time Play Console registration to reach (unlike iOS's Simulator path) -- that registration is cheap/one-time and isn't gated behind anything else, so it can happen well before the Apple side resolves.
     7. ✅ The public web build's access gate is built: signed-in-with-active-sync sees the real app, everyone else (signed-out or signed-in-without-sync) lands on `#marketingPage` (see History). Content-wise it's a first pass, not a final marketing page -- no real screenshots yet (none exist until there's a store listing to screenshot), no actual store badge links (nothing to link to yet either).
   - **What this remote session cannot do, and needs the user's own machine**: any actual Xcode or Android Studio build, the StoreKit Configuration simulation itself, real device/simulator testing, and both store accounts.
   - **Last, only after everything above works in sandbox/simulation**: Apple Developer Program ($99/yr) and Google Play Console ($25 one-time) signups, per the "Testing costs" note below -- both are correctly the very last thing paid for, not the first thing set up.
5. **Store compliance — not started.** Privacy policy + ToS (required once accounts/data leave the device), Apple privacy nutrition label, Play Data safety form, App Store Review Guideline 3.1.1 (StoreKit-only for this app's subscription — no external payment links are allowed for this category).
6. **Migration & rollout — not started.** Existing local-only users need a one-time "sign in to turn on sync" flow that uploads their existing local sermons rather than starting them empty. The no-account local-only path must stay fully functional as the permanent default, not a crippled trial.

**Testing costs**: most of phases 2–4 can be built and fully tested for $0 before committing any money — Supabase/Firebase free tiers are indefinite (not trials), RevenueCat is free under $2,500/mo tracked revenue, Capacitor is free/open-source, Xcode's local StoreKit Configuration file simulates the entire purchase/subscription lifecycle with no paid Apple account, and Android has free sideload testing plus reserved test product IDs for billing flows. The two genuinely unavoidable costs are the Apple Developer Program ($99/year, needed for TestFlight/App Store listing) and Google Play Console ($25 one-time, needed for any Play Store distribution) — both can and should be deferred to the very end of the roadmap, once everything else already works.

## Working conventions

- Development happens on a feature branch (`claude/app-store-paid-launch-1jkhur` as of this writing); the repo owner reviews/merges to `main` themselves. Don't push to `main` directly.
- No build step, no test framework in the repo. Verification during development has relied on headless Playwright scripts (spun up ad hoc against a local `python3 -m http.server`) to check layout, drag-and-drop, and console errors across viewport sizes — there's no persisted test suite, so re-verify UI changes manually/via a quick script rather than assuming coverage exists.
- Before making a UI/UX call that trades off discoverability, affordance, or live-presentation risk (anything touching Present mode especially), reason explicitly about who's affected and how — this project's design decisions have consistently favored a short written risk/tradeoff evaluation over silently picking a default.

## Backlog (user punch list, added 2026-07-21)

Given verbatim by the user, then triaged into an execution order (fastest/safest first, biggest open design decisions last). As each is finished, move it into History below and delete it from this list rather than marking it done in place, so this section always reflects only what's actually still outstanding.

1. **Present mode (mobile, reported on iPhone 17 Pro): the prev/home/next timer controls are disproportionately large.** Should scale with screen size instead of a fixed size -- max size is whatever they render at today (don't grow past that on a larger screen), min size is whatever stays legible at roughly a 12pt-equivalent font size (don't shrink past that on a smaller screen).
2. **Present mode (mobile, same device): the light/dark toggle is mostly off-screen.**
3. **Practice mode (mobile, same device): the note-related buttons are completely covered** (hidden/overlapped by something else in the layout).
4. **Rework how sync conflicts are surfaced and resolved.** First pain point identified and fixed (see History, 2026-07-22): conflicts were firing far more often than any real collision, since the trigger was pure timestamps with no check of whether the content actually differed. Still open, not yet addressed: even for a *genuine* conflict, the original card gives no indication it was just silently overwritten, and "Keep This"/"Keep Both" (shown only on the copy) don't make it obvious which version is which without opening both to compare.
5. **Flesh out a process for beta testers to test the full published app** before/around store launch — process/planning, not code.
6. **New-user tutorial content** (screen capture + voiceover, possibly AI-assisted) — content production, not code; this environment can't record a real device screen, so this needs either a recording done by the user with a script/storyboard provided, or a different approach discussed first.

## History

Dated as far back as the available commit history allows.

- **2026-07-06 to 2026-07-08** — Initial build of the app (manual file uploads: `index.html`, `sw.js`, icons, manifest), iterated to the point of being a working single-device PWA before this file existed.
- **2026-07-09** — Confirmed the paid-download + subscription-for-sync business model; decided to focus on app improvement before packaging/delivery work. Fixed Present-view toolbar overflow on phone widths. Self-hosted fonts (removed the Google Fonts CDN runtime dependency). Fixed two more responsive overflow bugs from a full breakpoint sweep. Extracted inlined base64 icons out of `index.html` into standalone files under `icons/`, with a light/dark variant naming convention. Redesigned the Prepare screen: sermon title moved into a create-time popup (reopenable from the top bar), formatting toolbar collapsed into a toggleable sidebar, per-section "+" buttons replaced by one toolbar "+" with drag-to-place. Switched wide-screen content columns from a centered `max-width` cap to fixed edge margins so content keeps growing on ultra-wide monitors. Evaluated and removed the visible scrollbar on Present-view content (accepted the live-presentation affordance risk as a deliberate tradeoff). Landed Phase 1 of the sync roadmap (UUIDs, tombstone deletes, schema version, device id, real backup merge logic). Laid out the full six-phase roadmap to backend/accounts/store packaging. Researched free-tier testing paths and the competitive/pricing landscape ahead of committing to the roadmap. Created this file and `README.md`.
- **2026-07-10** — Full visual/offline audit of the app against a "lofty" branding bar: fixed a service-worker precache bug left over from the icon-extraction pass (dead references to renamed files meant the bullet-list icon was never actually cached for offline use), an invisible-icon bug on the illustration FAB (its own gold "+" was blending into an identically-gold button background), a washed-out active-state nav icon, and a clipped date field on the sermon-info popup on phone widths. Replaced the placeholder toolbar icons (indent/outdent arrows, toolbar-toggle) with the user's actual designs, iterated twice more on bullet/numeral sizing in the list icons based on direct feedback, and fixed a missing flex-centering rule that had the icon-based toolbar buttons sitting flush left instead of centered. Evaluated the app against the competitive landscape and researched free-tier paths for the backend/native-wrapper phases. Landed five market-facing features, all offline and consistent with the local-first design principle: four more sermon templates (Verse-by-Verse Exposition, Life Application, Series Kickoff, Devotional/Reflection); a "Print Manuscript" export (full-content, all item types, via the browser's native print/PDF dialog, distinct from the scripture/illustration-only ProPresenter export); illustration usage tracking (`sourceIllustId` stamped on insert, surfaced as "Used in N sermons" so a pastor can see before reusing a story on the same congregation); full-text search across sermon titles/series/section titles/item text and illustration titles/body/tags; and an offline KJV scripture lookup (`data/kjv.json`, precached, public-domain text cleaned of embedded marginal notes) with a reference parser covering common abbreviation forms, letting "Insert KJV" turn a typed reference into real verse text with zero network dependency. Later the same day: made the template library user-editable (create/edit/delete/reorder sections, backed by a new `shema_templates_v1` localStorage key seeded from the built-in set, with a "Restore built-in templates" safety net) instead of a fixed, code-only list. Rebuilt the "Import" flow from a plain-textarea-only heuristic into a rich staging editor (`#importStage`) that accepts a real clipboard paste (bold/italic/underline/lists carry over from Word/Docs/Pages; every inline color and font is stripped since only the app's own CSS is allowed to set those) or an uploaded `.txt`/`.md`/`.html` file, with its own compact formatting toolbar and a manual "+ Section Break" control (real `<h1-6>` tags and the existing "Foo:" plain-text heuristic still auto-insert breaks) so a pastor can mark where each Prepare section should start before committing — replacing everything in the sermon only after an explicit confirm if it already has content. Fixed a CSS specificity bug this surfaced (an ID+type selector beat a same-specificity single-ID override) and a caret-placement quirk where inserting a `contenteditable=false` marker left the cursor before it instead of after. Also fixed the Present-view Play/Reset buttons not actually stretching to the timer's height above 768px (a later media-query rule was overwriting the `align-self:stretch` height with a fixed `42px` shared with `.icon-btn`) and added breathing room between the Library/Illustrations search bars and the title bar above them. Fixed the illustration "+" button's nested-ring look (`fabadd.png` was itself a circled plus, stacked inside the button's own gold-ringed circle — replaced it with a plain plus). Backup export/import now covers templates too (previously only sermons/illustrations), with the same id + `updatedAt` last-write-wins merge, so a custom template survives a device move. Illustrations gained an optional `imageData` field (base64, no backend): any `<img>` encountered during Import — a pasted image file, a `data:`/`blob:` src in pasted or uploaded HTML — is automatically split out into its own Illustration entry (title from alt text or filename) rather than silently dropping, with a thumbnail in the Illustrations list/picker and a "Remove Image" control in the edit modal. Immediately followed up on two implications of that image feature for the localStorage-only, no-backend architecture (flagged before shipping, not after): every image is now downscaled to a max 1600px edge and re-encoded as JPEG client-side before it's ever stored, so a full-resolution phone photo doesn't eat most of the local quota in one illustration; and `saveJSON()` (the one function every `persist*` call funnels through, including `persistTemplates` now) no longer swallows a failed `localStorage.setItem` silently -- it surfaces a toast ("storage is full, recent changes may be lost") the first time a save fails, deduped so a debounced autosave retrying against a still-full quota doesn't spam the same warning, and clears the moment any save succeeds again. Both were pre-existing gaps the image feature made much more likely to actually trigger, not new risks the image feature invented -- worth remembering if a future feature (e.g. attaching a PDF, or a longer audio note) adds another large-blob path, since neither fix is specific to images. Fixed the same missing-flex-centering bug (see the earlier icon-centering fix on the toolbar buttons) on `#fabAdd` itself -- its `<img>` was sitting off-center inside the button, independent of the earlier nested-ring icon fix. Added a manual "+ Add Image" control to the illustration form (upload/remove, same compression pipeline as auto-imported images), since until now the only way to attach a picture to an illustration was indirectly, through Import.
- **2026-07-20** — Started Phase 2 of the roadmap (Accounts & entitlement), committing to Supabase as the backend. Created the Supabase project and Google Cloud OAuth client, turned on Google + email auth, and verified both the project and the anon key directly against the live API before writing any app code. Specced the `profiles` table (the two entitlement flags, one row per user, auto-created via trigger) with RLS that's read-only for the client -- the client can never write `owns_app`/`sync_subscription_active` itself, since an updatable policy on those columns would let anyone grant themselves the paid unlock for free; that write only ever happens server-side (a future purchase-verification webhook using the service_role key). Wired the app side: self-hosted `vendor/supabase.js` (precached, keeping the zero-CDN-dependency convention), an Account modal (Google OAuth + email magic link, sign-out, entitlement display that degrades gracefully when no profiles row exists yet), and fixed an unrelated off-center `+` icon on `#fabAdd` noticed along the way (same missing-flex-centering pattern as the toolbar icons, this time on the button itself rather than the icon asset). Verified the whole flow with a route-intercepted mock Supabase client (page.route() rather than a plain init-script override, since the real vendored library's top-level assignment -- and, on the first load, this app's own reload-when-a-new-service-worker-activates behavior serving the real cached file on that reload -- both clobber a naively-injected mock) covering signed-out, signed-in-with-entitlements, and signed-in-with-no-profiles-row-yet, plus a genuine offline reload. A real Google OAuth click-through still needs a human in an actual browser to complete once the Redirect URLs allowlist covers wherever that test runs from. That human test happened the same day: hit a `redirect_uri_mismatch` first (the Authorized redirect URI on the Google OAuth client didn't exactly match Supabase's callback URL -- fixed by re-copying it from the Supabase dashboard rather than retyping it), then hit `ERR_CONNECTION_REFUSED` on the localhost redirect target because a work-computer policy blocked running a local server/port at all. Routed around that with GitHub Pages instead of a tunnel or local server -- the repo is already public, so Pages is free, requires zero local networking, and (serving from the feature branch rather than `main`) needed only a one-time Settings toggle, no code changes. Google Cloud Console needed no update for this (it only ever validates Supabase's own callback URL, never the app's origin); only Supabase's Redirect URLs allowlist got the new `https://rc3268.github.io/Shema/` address added alongside `localhost:3000`. With that, the full Google OAuth chain (Google Cloud → Supabase → app) was confirmed working end to end with a real account, closing out the auth half of Phase 2. Entitlement-writing itself (flipping `owns_app`/`sync_subscription_active` to true) is unstarted by design -- that's Phase 4's purchase-verification webhook, not this phase. Followed up the same day by building out the Account screen into a real account-management page and giving it a proper home: added a Settings screen (gear icon, replacing the old person-icon Account button) holding Account, Backup, Restore from Backup, and Dark Mode, and decluttered the Library header down to just "+ New" now that Backup/Restore moved. Deliberately did *not* move Import there -- it's a per-sermon authoring action, not an app-wide setting, and every account type needs it equally. Added a client-computed account-type concept (`local`/`dev`/`tester`/`sync`, no new schema) surfaced as a badge on the Account screen, plus the buttons a subscription screen is expected to have -- Manage Subscription, Restore Purchases, billing date/amount (shown only for `sync`), Delete Account -- all present and honest about not being wired up yet (informative toasts, not broken silent buttons) ahead of Phase 4. Much later the same day (after Phase 3's sync engine and its polish pass, both covered above): shipped the first slice of a new **Practice Mode** feature -- a separate "Practice" tab (own nav item, own `#view-practice`, own JS state/DOM ids throughout, deliberately not sharing anything with Present) where a pastor rehearses a sermon with a count-up timer and a local audio recording of the run-through, instead of Present's countdown-to-airtime timer. Chose a genuinely separate view over a mode-toggle inside Present specifically so recording/take-review controls could never have any chance of cluttering the view actually used for live preaching -- the two views share visual language only through already-class-based CSS (`.line`, `.timer-btn`, `.nav-btn`, the `--body-size`/`--title-size` custom properties) and a small extracted pure function (`buildStageItemsHtml`) for rendering a section's items identically in both, not through any shared ids or state. Recording uses `MediaRecorder`/`getUserMedia`; audio is kept in a new IndexedDB store (`shema_practice`, see "Current architecture snapshot") and never synced or uploaded anywhere. Keeps the last 3 takes per sermon (chosen over 1 -- too little to compare a week of rehearsal against -- or unlimited -- unbounded growth on exactly the browsers, notably iOS Safari, that are stingiest about PWA storage); recording a 4th silently drops the oldest, with a toast saying so rather than a silent surprise. Start/Pause/Resume are tied together with the recording (pausing the timer pauses the recorder via `MediaRecorder.pause()`/`.resume()`, not stop/restart, so one take stays one continuous file); "Discard" ends the session without saving; "Finish" stops, saves, and resets to 00:00. Leaving the view any other way mid-recording (tapping Home, switching to a different sermon's practice session) auto-finishes/saves rather than silently dropping the take, the same reasoning as the editor's autosave-on-exit. A "Past Takes" drawer (reusing the `.drawer-right` pattern) lists takes with inline `<audio>` playback and delete. Explicitly deferred to a later slice, per plan: highlighting a portion of the stage content, attaching a note to it, and pausing the timer/recording while doing so -- this slice is timer + recording + retention + playback only. Verified with Playwright using `--use-fake-device-for-media-stream`/`--use-fake-ui-for-media-stream` and a granted `microphone` permission: nav/empty-state, starting a sermon's practice session, timer counting up and correctly freezing across pause/resume, Finish saving a real take with a playable blob URL, the 4-takes-in-a-row retention cap dropping the oldest, Discard creating no take, switching sermons mid-recording correctly attributing the saved take to the sermon that was actually running, and a no-mic-permission context failing gracefully (toast, no crash, no orphaned take) rather than throwing. Confirmed via a direct screenshot comparison that a phone-width (390px) horizontal clipping artifact in the new top bar is not a regression -- Present's existing top bar shows the identical clipping at the same viewport width, so it's a pre-existing characteristic of that layout, not something Practice introduced. Followed immediately by Practice Mode's second slice: highlighting/notes with pause-on-annotate. Chose **item-level** annotation (tap a rendered item -- a paragraph, a scripture quote -- to flag it and attach a note) over arbitrary sub-item text-range highlighting: true character-range highlighting that survives re-renders and coexists with the item's existing rich-text markup is a genuinely hard persistence problem (offset schemes, partial-overlap handling, whitespace-normalization edge cases) for marginal extra value over item-level, since "things to change" in a sermon are naturally paragraph/item-sized edits anyway -- item ids the app already has for free. Tapping an item opens a small bottom-sheet note popup (excerpt of the item's text for context, a textarea, Save/Delete); if the session was running, opening the popup pauses both the timer and the `MediaRecorder` (`.pause()`, not stop -- same take continues after), and closing the popup resumes both again *only if this exact popup is what paused them* -- closing it after the user had already manually paused beforehand correctly leaves it paused rather than force-resuming. Notes persist in a new `shema_practice_notes_v1` localStorage key (`{sermonId, sectionId, itemId, excerpt, note}`) -- plain text, small, so unlike audio takes they don't need IndexedDB or a retention cap. Noted items get a small gold dot badge (`.line.has-note::before`) rather than a background-color highlight, specifically so it doesn't visually collide with the scripture/illustration item types' own colored left-border treatment. A "Notes" drawer (same drawer-right pattern as Past Takes) lists every note for the sermon with a "Go" button that jumps `practiceCurrent` to the right section. Isolation from Present required one shared-code change: `buildStageItemsHtml` (used by both views) now stamps an inert `data-item-id` attribute on every `.line` div -- harmless for Present (nothing there reads it), but it's what lets Practice's click delegation and has-note post-processing target the right item without Present's rendering path ever being touched. Verified with Playwright: tapping an item mid-recording freezes the timer for the full duration the popup is open; saving resumes it and the timer keeps advancing past where it would've been; re-tapping a noted item reopens pre-filled with Delete visible; closing after a manual pause does not auto-resume; the notes drawer's jump and delete both work; and a note survives a real page reload. Followed by an efficiency/security self-audit (no new feature ask -- a direct request to review the app for bloat and for how well it protects the user's data). Findings and fixes: added a `Content-Security-Policy` meta tag (`default-src 'self'`, `'unsafe-inline'` for script/style since the app is one inline `<script>`/`<style>` block with no build step to hash or nonce them, `data:`/`blob:` allowed for the base64 illustration images and in-memory audio blob URLs, and the Supabase host as the only non-self `connect-src`) as a defense-in-depth layer in case the rich-text sanitizer is ever bypassed -- `frame-ancestors`/`report-uri` deliberately omitted since meta-tag CSP delivery ignores them per spec and listing them would be misleading. Added a root `.gitignore` (previously none existed). Closed a real data-availability gap flagged by the audit: practice takes (IndexedDB audio) were the one kind of user data with zero export path -- unlike sermons/illustrations/templates they're deliberately excluded from the JSON backup (rehearsal audio never leaves the device by design), which meant a cleared browser or a device switch could permanently and silently erase rehearsal history. Added an "Export" button next to each take in the Past Takes drawer that downloads that one recording (`.webm`/`.m4a`/`.ogg` depending on the browser's actual `MediaRecorder` output, named from the sermon title + date) via a plain `<a download>` on the take's existing blob URL. Building this surfaced a real regression in the same drawer: adding a third element (Export, alongside the existing Delete button and the `<audio>` player) to the take row's single flex line no longer fit in the drawer's fixed ~400px width -- `.practice-take-info`'s `flex:1; min-width:0` collapsed all the way to zero and its text visually overlapped the audio control instead of just being tight. Fixed by splitting each take row into two stacked lines (info + buttons on top, audio player full-width below, via a new `.with-audio` modifier scoped only to Takes rows so the Notes drawer's simpler two-button rows are untouched) rather than trying to squeeze everything narrower -- more robust against any future button added to this row, too. Verified with Playwright: no CSP-violation console messages across normal use (icons, self-hosted fonts, the illustration data: image, the practice audio blob, `fetch('data/kjv.json')`), the export button produces a real downloaded file with a sensible filename, direct `getBoundingClientRect` measurements confirming the info-text-overlap bug is gone (info text now keeps real width instead of collapsing to zero), and a full re-run of every existing Practice/Settings/sync Playwright script to confirm nothing else regressed.
- **2026-07-21** — A whole-app homogeneity pass (style/format/function consistency, requested directly rather than surfaced by a specific bug report): confirmed several things are already consistent app-wide and don't need touching (the drawer-right-vs-bottom-sheet modal split matches its documented rule with zero exceptions across all 13 modals; every modal close button uses the same icon-close asset; zero hardcoded colors in any inline style anywhere; a deliberate two-tier button language of icon+label chrome vs. text-only contextual actions holds everywhere). Fixed three concrete inconsistencies found: (1) the Templates list row's Delete button had a trash icon while its siblings in the same row (Edit/Keep This/Keep Both, same `.icon-sm` class) were plain text -- removed the one-off icon rather than adding icons to the other three, matching the dominant plain-text convention every other card type (sermon, illustration) already uses for the identical action. (2) `Sync Now` was rendering a raw "↻" Unicode character, the only icon-shaped control in the app not using a real icon asset -- font-glyph rendering is inconsistent across OS/browser/webview in a way a designed icon isn't, which matters once this is wrapped for app stores. Replaced with a self-contained inline SVG (Feather's refresh-cw shape) using `stroke="currentColor"` so it adapts to light/dark automatically with no separate asset files needed -- not a hand-drawn match for the rest of the icon family, but removes the actual technical inconsistency; flagged as a placeholder if a pixel-matched asset is ever produced. (3) Prepare had no empty state -- Library, Illustrations, Present, and Practice all show a friendly guidance message when there's nothing to show, but tapping the always-live "Prepare" nav tab with no sermon open left a blank canvas with the Save/Export/Present toolbar still visible and looking active. Added `renderEmptyEditor()` (mirrors the wording style of Present's/Practice's own empty states) wired into `showView('editor')` when `getEditing()` has nothing to return, hiding `#richToolbar`/`#editorFooter` until a real sermon is opened; `renderSections()` un-hides both again the moment one is, so the normal Edit/+New flow is unaffected. One flagged inconsistency deliberately left open, not fixed: the Practice tab's bottom-nav icon is a generic geometric stopwatch SVG, visibly thinner/more generic than its five hand-drawn siblings (Library/Plan/Prepare/Illustrations/Present) -- this needs an actual matching custom asset, which isn't something to fabricate procedurally; still a known gap pending real icon art. Verified all three fixes plus a full existing-suite regression pass with Playwright: the empty-editor state appears on direct nav with nothing open, correctly disappears (and the toolbar/footer reappear) the instant a real sermon opens, and survives navigating away and back while a sermon is open; the template row's Delete button is confirmed icon-free; the Sync Now button is confirmed to render an `<svg>` in both themes.
- **2026-07-21 (later the same day)** — Started Phase 4 for real: scaffolded Capacitor. Bundle id decided with the user: `com.shema.app` (generic, no company name tied to it). This is the first commit to introduce an actual build step -- `package.json`, `node_modules/` (gitignored), a `capacitor.config.json` -- to what has otherwise been a zero-build-step project by design; the web/PWA build is unaffected and still needs nothing but the repo's static files, since Capacitor's `webDir` (`www/`) is just those same files copied verbatim, never transformed. `www/` itself is gitignored and regenerated by `scripts/build-www.js` (a plain file copy, no bundler) via `npm run build`; `npm run sync` runs that and then `npx cap sync`. Both `ios/` and `android/` native projects are committed (standard practice for Capacitor -- they hold real project config, not purely-regenerable output). iOS added cleanly with zero CocoaPods/Ruby dependency -- this Capacitor version uses Swift Package Manager instead, which meant the whole scaffold (both platforms) completed in this Linux container with no Xcode or Android Studio installed, something not assumed possible going in. Added the permission declarations Practice Mode's existing `getUserMedia` call will need once wrapped: `NSMicrophoneUsageDescription` in iOS's `Info.plist`, `RECORD_AUDIO` in Android's manifest -- neither is enforced by a browser PWA, so both would have silently failed the first native run without this. Replaced Capacitor's generic placeholder icon/splash with Shema's real mark: `scripts/gen-native-icons.py` (Pillow, since `@capacitor/assets` depends on a prebuilt `sharp` binary that 403'd through this environment's proxy -- worked around it directly instead of fighting that download) regenerates every Android density's launcher/adaptive-icon PNGs and iOS's single 1024×1024 universal icon from the existing `shema-icon-512.png` (already maskable-safe per its own manifest.json entry, confirmed by sampling a corner pixel against the center -- both are solid `--forest`, meaning it already fills its own safe zone edge-to-edge with no transparent margin to worry about), plus a splash screen for both platforms built by centering `shema-mark-color.png` on a `--cream` canvas at every size Capacitor's scaffolding had already generated placeholders for. Caveat worth remembering before final store submission: the source icon is only 512px, upscaled to 1024px for iOS -- fine as a placeholder, but a true 1024px (or larger) master would look sharper. Left open, not done in this pass: verifying the CSP's `'self'`-scoped `connect-src`/`img-src` actually resolves correctly against Capacitor's real WebView origin (`capacitor://localhost` on iOS, `https://localhost` on Android) -- reasoned through as likely fine since Capacitor serves bundled assets as same-origin, but nothing here can substitute for an actual device/simulator run, which needs Xcode/Android Studio this environment doesn't have. Also not attempted: any real Gradle or `xcodebuild` compilation -- `cap add`/`cap sync` only ever copy template files and web assets, they don't compile anything, so "it scaffolded without errors" is not the same claim as "it builds."
- **2026-07-21 (later still)** — RevenueCat client integration + the purchase-verification webhook, the two pieces Phase 4's plan called "buildable for $0." **Real gap, not closed this pass**: there is no RevenueCat project yet -- that needs an actual human signup, which can't happen from here. Everything below is real, working code and is fully unit-tested, but is unverifiable end-to-end until a real RevenueCat project with real products exists; the two API-key constants in `index.html` are deliberately empty placeholders, and every call path checks for that and no-ops with a clear toast rather than pretending to work. Surfaced, not resolved, a real open pricing/distribution question found while wiring this: context.md's business-model note ("one-time purchase: local-only use... this is the base product and must stay fully functional forever") reads equally well as "a free download with an in-app unlock" or "a traditionally paid-up-front store listing" -- the two imply different things about whether anything should ever be feature-gated pre-purchase. Deliberately did not decide this -- nothing built gates any existing feature on entitlement (matching how the Account screen already only ever *displayed* entitlement, never enforced it), so this stays reversible either way until it's actually decided.
  - **Client side** (`index.html`): two entitlements, matching the two Postgres flags exactly -- `owns_app` (checked two ways OR'd together: the signed-in account's Supabase profile flag, so a purchase follows you to a second device/platform; *and* this device's own local RevenueCat entitlement, checked independently, so the one-time unlock keeps working with no account at all, per the "no account, forever" model) and `sync` (inherently account-bound, since sync needs an identity to attach cloud data to -- only ever checked via the Supabase flag). Added an "Unlock the full app" row+button to the Account drawer, shown above the sign-in split for exactly that reason (unlock isn't sign-in-gated), and a "Start Sync" button next to the existing signed-in-only sync status row. `acctManageSubBtn`/`acctRestorePurchasesBtn` (Phase 2 placeholders) now do real things on native: Restore calls `Purchases.restorePurchases()`; Manage deep-links to the platform's own subscription page (`apps.apple.com/account/subscriptions` / Play's equivalent) since neither store SDK exposes an in-app "show the manage sheet" call through this plugin. `RevenueCatPurchases.logIn()`/`logOut()` are kept in step with Supabase sign-in/out so a purchase's identity always matches whichever account (if any) is active.
  - **Self-hosting the plugin, same convention as `vendor/supabase.js`**: `@revenuecat/purchases-capacitor` only ships an ESM build, no CDN-ready bundle, so `scripts/vendor-src/purchases-entry.js` + `npm run build:vendor-purchases` (esbuild, IIFE) produce `vendor/purchases-capacitor.js`, exposing `window.RevenueCatPurchases`. Loaded via a conditional `document.write` right after `vendor/supabase.js`, gated on `window.Capacitor.isNativePlatform()` -- true only inside the native shell, so this is a zero-extra-download no-op for every ordinary web/PWA visit (and deliberately not added to `sw.js`'s precache list, for the same reason: the web build never uses it). Confirmed with Playwright: the script is never requested on a plain web load, and all three buttons show the correct "not available on the web" toast instead of erroring.
  - **The webhook** (`supabase/functions/revenuecat-webhook/`): a Deno Edge Function, the only thing ever allowed to write `profiles.owns_app`/`sync_subscription_active` (unchanged from Phase 2's RLS design -- this just finally builds the writer that policy was always waiting for). Deliberately does *not* hand-interpret RevenueCat's dozen-plus event types (INITIAL_PURCHASE, RENEWAL, BILLING_ISSUE, PAUSE, ...) to guess current entitlement state -- it re-fetches the subscriber's canonical status from RevenueCat's own REST API on every event and writes that instead, so grace periods/retries/refunds can never drift out of sync with what this function decides. Anonymous RevenueCat ids (`$RCAnonymousID:...`, from a purchase made with no Shema account) are detected and skipped -- there's no `auth.users` row to attach that purchase to; it stays tracked purely on-device, by design. Auth is a single shared-secret `Authorization` header (`REVENUECAT_WEBHOOK_AUTH`), checked exactly, matching what RevenueCat's own dashboard lets you configure per-webhook. Needs three things set before it's live, none of them done here (no CLI/credentials in this environment): `supabase functions deploy revenuecat-webhook`; `supabase secrets set REVENUECAT_WEBHOOK_AUTH=...` and `REVENUECAT_SECRET_API_KEY=...` (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are already auto-injected, nothing to set there); and pointing RevenueCat's dashboard webhook config at the deployed URL with a matching Authorization header value.
  - **Verified**: `index.test.ts` (Deno's test runner, `globalThis.fetch` mocked so no real network/project is needed) covers the auth check, the anonymous-id skip, both entitlements computed correctly from a mocked subscriber payload (including an expired-vs-active sync subscription), and the upstream-failure/non-POST paths -- 10/10 passing. On the client side, confirmed via Playwright that the web build stays fully inert (no plugin script fetched, all three billing buttons toast correctly) and the full existing Settings/Account/Practice/CSP regression suite still passes untouched.
- **2026-07-21 (pricing/distribution decision)** — Fixed a real, visible CSS bug found along the way: every `drawer-right` modal (Settings, Account, Past Takes, Practice Notes, History, Import) stays in the DOM at `display:flex` even closed, translated 100% of its own width offscreen -- but `box-shadow` isn't clipped by an element's own transform, so each sheet's shadow was still bleeding ~36px back into the viewport at that exact closed position, all six stacking into a permanent dark strip down the right edge of every screen. Fixed by pushing the closed transform an extra 40px so the shadow clears too. Then spent real time re-deciding pricing/distribution from first principles rather than just filling in the numbers the earlier competitive research had left as placeholders -- the app is far more built out now (Practice Mode, the full sync engine, exports, templates) than when that research happened, and the free+IAP-unlock plan just wired up (see the previous History entry) turned out to have a real flaw once pressure-tested. The reasoning, in order: started from "free download + IAP unlock" (the initial recommendation, on the theory that pastors are a slow, trust-driven, hard-to-discover buyer who need zero friction to even look) → the user pushed back that a permanently-limited free tier risks the brand being known as "the free minimally-functional tool," and that gating real features behind an undisclosed trial reads as exactly the kind of bait-and-switch this app shouldn't resemble → reconsidered toward a fully-featured, sermon-count-bounded trial (first 2 sermons, everything unlocked, transparently) as a fix for the disclosure problem specifically → then reconsidered the *discovery* premise itself: pastors mostly arrive via referral/demo/word-of-mouth, not cold App Store browsing, so "free removes browse friction" was weighted too heavily to begin with -- and utility apps that gate functionality behind a trial draw a disproportionate, permanent share of "you have to pay to use this?!" one-star reviews, a sharper risk for a brand-new app than the discovery benefit was worth → landed on a genuine paid store listing, no trial, no in-app unlock at all for the base app. That still left one open thread: whether the web build should keep being a free-forever, no-account, fully-functional alternative (the original plan) sitting right next to a *paid* native app -- the user correctly called this out as self-defeating (why would anyone pay for the app if the identical thing is free at a public URL) and settled it directly: no standalone free web product; the web build's only ever gate is a signed-in account with an active sync subscription, which as a side effect means the sync subscription's whole justification changed from "cross-device between two phones" to "the only way this ever touches a computer" -- clearer, and no longer a price that needs judging against the one-time unlock at all. Landed, finalized this pass: **native app $19.99 one-time (real store listing, not IAP)**, **sync $4.99/mo or $39.99/yr (unchanged)**, **the public web URL becomes a marketing/landing page** for anyone not signed in with active sync. Full reasoning captured in the rewritten Business model section above; this entry is the paper trail for *why*, since the conclusion looks different enough from the original plan that future sessions should understand it was reconsidered, not just amended. **Not yet done, flagged in the Roadmap above**: removing the now-superseded `owns_app`/local-unlock RevenueCat code from `index.html` and the webhook, and building the actual marketing-page/sign-in-gate behavior for the web build.
- **2026-07-21 (implementing the finalized model)** — Both flagged items above, done. Removed all `owns_app`/local-unlock code: the `RC_ENTITLEMENT_UNLOCK` constant, `getLocalEntitlement()`, the "Full unlock" row/button in the Account drawer, and the corresponding half of the webhook -- `RC_ENTITLEMENT_SYNC` (and the `sync` Postgres flag) is now the only entitlement RevenueCat or the webhook ever touches; `purchaseEntitlement()` stayed generic (still used by "Start Sync") since there was no reason to un-parameterize it for one caller. Built the web access gate: `#marketingPage` (hero, two feature call-outs leading with Present mode and Practice Mode specifically -- the two things a $2.99 notes app or a free competitor can't touch, per the pricing discussion -- and a sign-in box) now sits as a sibling of `#app`, never nested inside it, so toggling one never touches the other's markup. `decideWebAccess()` shows `#app` only to a signed-in account with `sync_subscription_active` true; everyone else on web gets the marketing page; native always skips the check entirely and shows `#app` immediately (the purchase there is a store listing, nothing to verify). Real bug caught before it shipped, worth remembering: the naive fix (`element.style.display = ''` to "un-hide" `#app`) doesn't actually override the CSS class rule that hides it by default on web (`html.is-web #app{display:none}`, added specifically so the real app can't flash visible for a moment before the async Supabase check resolves) -- an empty inline value only clears an inline override and falls back to the cascade, where the class selector still wins over the bare `#app{display:flex}` rule. Needed an explicit `'flex'` instead. Caught by testing the exact computed style at three different session states (signed-out, signed-in-without-sync, signed-in-with-active-sync), not just eyeballing a screenshot. Also worth remembering for future testing: a crude `window.Capacitor` mock in a test gets silently overwritten the moment `vendor/purchases-capacitor.js` loads, since that bundle carries `@capacitor/core`'s own real `isNativePlatform()` -- which correctly notices the mock isn't a real native bridge and reports `false`. Not a real bug (an actual native runtime injects `window.Capacitor` before any page script and never has this problem) but it produced a confusing false test failure until traced down; the fix in tests going forward is to block that one script request when mocking native mode. Verified: the flash-of-real-app case (computed `display` checked immediately on load, before the async check could plausibly resolve), all three gate states rendering correctly (including via a real signed-in-with-active-sync session mocked through a seeded `sb-<project-ref>-auth-token` localStorage entry plus a routed `profiles` REST response, not just a native-mode shortcut), and a full re-run of the existing regression suite -- which mostly needed one small update to keep testing what it always tested: since `#app` now requires either native or an active-sync web session to reach at all, tests exercising unrelated features (Settings, Practice Mode, sync conflicts, CSP/export) now mock native mode to get there, which is arguably more honest anyway since the native app is the actual shipping product.
- **2026-07-21 (RevenueCat project setup, live now)** — Walked the user through actually creating the RevenueCat project rather than just planning it. Real progress, real blockers hit exactly where expected: the Android app, the `sync` entitlement, and a `sync` Offering with monthly/yearly packages are built; the iOS app is genuinely stuck on App Store Connect's In-App Purchase key requiring an active (paid) Apple Developer Program membership -- confirmed by hitting that wall directly, not just predicted -- so iOS stays deferred until that membership exists. Corrected one real setup mistake before it caused confusion later: initially entered as two unrelated products (`sync_monthly`/`sync_yearly` as separate Subscriptions), corrected to Google Play's actual model of one `sync` Subscription with two Base Plans (`sync-monthly`/`sync-yearly`, hyphens not underscores -- Play Console's base plan id field doesn't allow underscores) -- letting a user switch monthly/yearly within one subscription rather than juggling two disconnected ones. Got a real Android public API key (`goog_...`) and wired it into `index.html`'s `REVENUECAT_API_KEY_ANDROID` (iOS's stays empty, still blocked). `rcReady`/`initRevenueCat()` will now actually attempt to configure the SDK on a real Android device -- unverified beyond that, since this environment has no device/emulator to run it on. Re-confirmed the `window.Capacitor` mock-clobbering test artifact (documented in the previous History entry) reproduces identically on a fake Android platform, not just iOS, for the same reason -- purely a test-mock fidelity issue, not a code bug, worth remembering next time a native-mode test needs writing. Still open: the RevenueCat webhook secret + REST secret key (waiting on finishing the RevenueCat dashboard side), deploying the Edge Function, and everything downstream of the Apple Developer Program membership.
- **2026-07-21 (Privacy Policy + Terms of Service drafted)** — Started Phase 5 (store compliance). Decided where these should live first: same GitHub Pages site as the marketing page, as plain static files (`privacy.html`, `terms.html`) in the repo, not a separate "real" website -- Apple/Google only require a stable public URL for these, not a custom domain, and this keeps the zero-build, single-host convention intact. A custom domain (cheap, ~$12/yr, just a CNAME onto the same GitHub Pages site) stayed an explicit "later, once sales justify it" upgrade rather than something to do now, since it's purely a trust/polish improvement for the slow, trust-driven pastor buyer, not a functional requirement. Drafted both pages grounded in what the app actually does, not generic boilerplate: local-only use collects nothing at all; signing in for Sync sends sermon/illustration/template content and account email to Supabase; practice recordings never sync or upload under any circumstance; purchases/subscriptions are handled entirely by Apple/Google via RevenueCat, with Shema never seeing payment details; no analytics SDKs or ad networks anywhere in the app. Two fields are left as visible red placeholders pending real values before publishing -- a support contact email and the governing-law jurisdiction for the Terms -- flagged clearly in the page content itself and called out to the user rather than filled with a guess. Linked both from a new small `.mkt-legal` footer on `#marketingPage`, added both to `sw.js`'s precache list (bumped to `v68`) for the same offline-first reasoning every other app asset gets, and verified with Playwright that the footer links render and navigate correctly and that both pages load cleanly. Not yet done: filling in the two placeholders, and reconciling this content with Apple's App Store privacy "nutrition label" and Google Play's Data safety form once those are actually filled out (they ask the same questions in a structured format and need to agree with what these pages say).
- **2026-07-21 (RevenueCat webhook deployed and verified live)** — Finished wiring the Android-side RevenueCat project into a fully working, verified webhook end to end, closing out everything Task #32 can reach in this environment (the iOS half of the RevenueCat project itself stays blocked on the Apple Developer Program, per the previous entry). Generated the RevenueCat secret API key (chose V1, matching the webhook's existing `/v1/subscribers/{id}` call and response shape, over V2 which would have needed a rewrite) and a random webhook shared-secret (`openssl rand -hex 32`), both set as Supabase Edge Function secrets (`REVENUECAT_SECRET_API_KEY`, `REVENUECAT_WEBHOOK_AUTH`) via the dashboard UI -- never written into any file or committed; flagged to the user that the secret key should be rotated since it was pasted into chat before being stored safely. Deployed `supabase/functions/revenuecat-webhook/index.ts` via Supabase's dashboard function editor (paste-and-deploy, no CLI available or needed) and turned off its "Verify JWT" toggle, since RevenueCat's webhook calls carry no Supabase JWT and the function already does its own Authorization-header check. Verified in three independent stages rather than trusting any single one: (1) manual curl against the live URL confirmed the expected 405→401→401→200 auth-check progression; (2) a curl with a syntactically-valid-but-nonexistent Supabase user id correctly failed with a Postgres `23503` foreign-key violation (confirmed via Edge Function logs) -- the expected, healthy outcome, proving the RevenueCat secret-key lookup and the `profiles` upsert both work and the FK constraint is correctly rejecting bogus writes rather than silently accepting them; (3) registered the same URL + Authorization header inside RevenueCat's own Integrations → Webhooks (Both environments, All apps, All events, paywall events unchecked) and used RevenueCat's own "Send test event" feature, which returned a real HTTP 200 with body `ok (test event)` -- confirming the integration is genuinely live inside RevenueCat's system, not merely reachable by hand. Remaining before Task #32 can fully close: a real Play Console developer account/subscription registration (RevenueCat's Android app so far only reflects a RevenueCat-side placeholder, not an actual registered Play Store product) -- deliberately deferred, per the roadmap's "store accounts last" ordering, since it's the one piece of this task that costs real money ($25 one-time).
- **2026-07-21 (mobile phone screen-size polish pass)** — Prompted directly by the user hitting a real bug on their own phone ("can't read the tool names... they clip over each other") while testing the sync-gated web build on an iPad/phone browser -- not a hypothetical, an actual reproduction. Fixed the immediate report first (the bottom nav: `#bottomnav`), then used it as the occasion for a fuller pass, screenshotting every major view and modal at a 390px-wide phone viewport with Playwright rather than guessing from the code. Also folded in a same-day, separately-reported fix: illustration card/preview/picker thumbnails (`.illust-thumb`) were using `object-fit:cover`, cropping images to fill their fixed width×160px box instead of respecting the original aspect ratio -- switched to `object-fit:contain`. Real bugs found and fixed by the sweep itself, roughly in order of severity:
  - **Bottom nav label overlap (the reported bug)** -- `.nav-item` had no `min-width:0`, so on a narrow screen "Illustrations" (the one long label among six) grew past its even 1/6 share at every other tab's expense, and since its text had no wrap/ellipsis handling either, it visually spilled into "Practice" with zero gap between them. Fixed both halves together: `min-width:0` on `.nav-item` forces an even split, and each label is now wrapped in a `<span class="nav-label">` with `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` so if a label still doesn't fit, it degrades to "ILLUSTRA…" instead of overlapping its neighbor. Verified equal widths and zero `scrollWidth` overflow across all six tabs at both 390px and 360px.
  - **Sermon-card action buttons overlapping the title/tag/date column** -- `.sermon-card` is one flex row with `.info{flex:1;min-width:0}` beside `.actions{flex-shrink:0}`; four action buttons (Present/Practice/Edit/Delete) never shrink, so on a phone `.info` got squeezed down to a couple of characters (title rendered as "Th…") while the tag pill visually collided with the button row. Fixed with a `@media(max-width:480px)` rule that stacks the card (`flex-direction:column`) and gives the actions row `flex:1` buttons so they split the full card width evenly on their own line below the title -- unaffected above 480px.
  - **Scripture item's "Insert KJV"/"Look up" buttons running off the right edge of the screen** -- in the editor, `.item-row` laid the drag handle, type select, text box, both scripture buttons, and delete button all as flat siblings in one unwrapped flex row; on a phone the combined minimum width of six items exceeded the viewport and the trailing "Look up" button was pushed half off-screen with no way to reach it. Restructured so the text box and its scripture-only buttons share a new `.item-main` wrapper (flex:1, separate from the row's other fixed-width chrome), which at ≤480px switches to `flex-direction:column` -- text takes the full row, buttons wrap to their own line beneath it, each shrunk slightly (12px, `white-space:nowrap`) so they still read as one line even with the formatting-toolbar sidebar open eating extra width.
  - **Present/Practice section title clipping off-screen entirely** -- `#sectionTitle`/`.practice-section-title` render at `var(--title-size,44px)`, and section names are usually one word ("INTRODUCTION") with no internal space to wrap at -- default `overflow-wrap:normal` let it overflow past `#stage`'s `overflow:hidden` edge and vanish rather than resize or wrap, silently dropping half the word ("INTRODUCT" with "ION" gone) on a 390px screen. This is the view a pastor is actually reading from mid-sermon, so a silently truncated section label is a real live-presentation risk, not just cosmetic. Fixed with `overflow-wrap:break-word` on both the section-title and sermon-cover-title elements (Present and its Practice-mode twin) -- lets a too-long single word break onto a second line instead of disappearing; multi-word titles already wrapped fine and are unaffected.
  - **Practice mode's "Finish" button reachable only by discovering a horizontal scroll** -- `.practice-top` already had `overflow-x:auto` as a deliberate fallback for its six controls (Start/timer/Discard/Notes/Takes/Finish/dark-toggle) not fitting on one phone-width row, but that left Finish -- the button that actually stops and saves the take -- scrolled off the right edge by default with only a sliver ("Fin") visible as the sole hint more existed. Restructured so everything except Finish lives in a new inner `.practice-top-scroll` wrapper that scrolls on its own; Finish sits outside it, pinned, `flex-shrink:0`, always fully visible regardless of screen width.
  - Verified the full set with Playwright screenshots at both 390px and 360px viewports across Library, Plan, Prepare (including the formatting-toolbar sidebar open and the sermon-info popup), Illustrations (list + add form), Present, Practice, Settings, and Account, plus a re-run of the existing CSP/export and RevenueCat-web-gate regression scripts to confirm nothing else broke.
  - Mid-task note for future sessions: this pass's `index.html` edits were lost once, uncommitted, to an environment outage mid-turn, and had to be redone from the diagnosis already worked out in conversation -- worth remembering that uncommitted edits in a long working session aren't safe from that until actually committed, so committing promptly after a verified fix (rather than batching many fixes before the first commit) is the safer default going forward.
- **2026-07-21 (two more phone/tablet reports: nav icon recolor, iPad bottom whitespace)** — Two more real reports from the user's own devices. (1) In light mode, the active bottom-nav tab's icon recolored gold along with its label, but the gold-via-`filter:invert/sepia/hue-rotate` trick that works for dark mode's icons was illegible against light mode's icon art -- removed the filter for light mode entirely (`.nav-item.active .ic` no longer sets one; only `color` on the label still changes), leaving dark mode's `body.dark-mode .nav-item.active .ic` filter untouched. (2) A persistent sliver of mismatched background below the bottom nav on iPad specifically -- root cause: `#app`/`#marketingPage` computed their height via a separate `100vh; 100dvh` calc, while `body` is already `position:fixed; inset:0` (pinned pixel-exact to the real visible viewport). Two independent viewport-height calculations can round a fraction of a pixel differently, especially at higher device pixel ratios, exposing a hairline of `html`/`body`'s background right where the nav's own background should reach the screen edge. Fixed by changing both to `height:100%`, inheriting body's already-exact box instead of recomputing it a second time. Caveat honestly noted to the user: this environment can't reproduce Safari/WebKit's actual viewport-chrome behavior to screenshot the "before," so this was verified by removing the redundant calculation and confirming no regression in the existing gate/RevenueCat/CSP test suite, not by reproducing the exact artifact first -- worth a real-device recheck.
- **2026-07-21 (comprehensive mobile/tablet audit, prompted by "would React fix our formatting problems?")** — The user asked whether the app's recurring layout bugs meant it should be rewritten in React. Answer given: no -- every bug found and fixed this week (nav overlap, button stacking, section-title clipping, the iPad whitespace above) is a CSS/flexbox mistake, not a state-management problem, and the same mistakes are exactly as easy to write in JSX; the actual gap was that views only got tested reactively, one report at a time, rather than swept systematically. Built a Playwright audit (`full-audit.js` in scratchpad) across 4 breakpoints (360×780, 428×926, 768×1024 iPad portrait, 1024×768 iPad landscape) × light/dark, walking all 6 main views and all 13 modals with deliberately worst-case seed content (a long sermon/series/illustration title, a long unbroken word, a long scripture reference, many illustration tags), with a programmatic overflow/clipping detector rather than eyeballing screenshots. First run flagged literally every screen -- turned out to be a bug in the audit script itself, not the app: closed `drawer-right` modals (Settings, Account, History, Import, Practice Takes/Notes) stay laid out in the DOM at all times, hidden by sliding the sheet off-canvas via `transform` rather than `display:none` (so a CSS transition can animate the slide-in) -- the detector's "does this element's right edge exceed the viewport" check doesn't know that off-canvas position is by design, not a bug. Fixed by excluding any element whose closest `.modal-overlay` ancestor lacks the `.open` class. The corrected re-run found one genuine, previously-undiscovered bug: `.line` -- the body-text rendering for every item on the actual Present/Practice delivery stage, as opposed to the editor's own contenteditable text box -- had no `overflow-wrap`, so a single long unbroken word (a name, a URL, a technical term) would silently clip past `#stage`'s hidden edge on phone widths, same failure mode as the section-title bug fixed in the previous pass but never applied to the item body content itself. Fixed with the same `overflow-wrap:break-word` treatment. Separately worth remembering for future audit scripts: this app's inline `<script>` block is one big IIFE (`(function(){...})()`), so its internal functions (`openTemplateEditor`, `openIllustPicker`, `showConfirm`, `deleteSermon`, etc.) are **not** global/`window`-attached the way a plain non-wrapped script's top-level functions would be -- a `typeof fn === 'function'` guard around a direct call to one of these from `page.evaluate()` silently evaluates false and no-ops rather than throwing, which is exactly what happened to 4 of the audit's 20 "screens" (illustration picker, template editor, practice note popup, the delete-confirm dialog) -- they never actually opened the modal they were meant to test and just re-screenshotted whatever view was already showing. Not yet gone back to re-verify those 4 specific modals through real UI-driven triggers instead of direct calls; flagged here rather than silently left as "audited" when they weren't.
- **2026-07-21 (real user-hit bug: sync permanently wedged by a restored old backup)** — The user reported a live incident, not a hypothetical: a sermon written on PC wasn't appearing on a signed-in iPad even after hitting Sync, so they restored a JSON backup file from a very old build onto the iPad hoping to bring it over manually -- after that, every sync attempt (automatic or manual) on the iPad failed, while the PC kept syncing fine. Root cause, found by reading the sync engine rather than guessing: `runSync()` wraps its loop over all three tables (`sermons`/`illustrations`/`templates`) in a single `try/catch`; any one record's processing throwing, or even just one record's Supabase `upsert` returning a permanent rejection, sets the whole pass's `ok` to false, and since a failing record is (by definition) never marked synced, it gets retried -- and re-fails -- on every future pass, forever. This matches "any sync gives an error" exactly: whatever incompatible field the very old backup's sermon carried, Postgres would keep rejecting it every single time, and (before this fix) a JS-level throw during that processing could have taken out every *other* record and table in the same pass too, not just the bad one. Three fixes, verified together with a Playwright test that mocks Supabase to permanently reject one specific record's id: (1) `syncOneTable`'s pull and push loops now isolate each record in its own `try/catch`, so one bad record can't block anything else in the same pass -- confirmed a healthy sermon and a different table's illustration both still sync in the same pass as the failing record; (2) `deleteSermon`/`deleteIllustration` now check `syncedAt(table,id)` -- a record that has *never* successfully synced has no remote row to resurrect on a future pull, so it's spliced out of local storage entirely instead of tombstoned; a tombstone would still carry the same rejected fields and keep failing forever, so simply deleting the bad restored sermon through the normal UI is now the actual recovery path (confirmed: after deletion, the next sync pass never retries that id again); (3) `normalizeSermon` now coerces a non-array `sections` (or a section's non-array `items`) to empty instead of letting `.forEach` throw on it -- a separate, more severe failure mode than the sync error itself (a foreign/corrupt backup shape at that level crashes the *entire app* at load, not just sync, since `sermons.forEach(normalizeSermon)` runs unguarded during init) that a "restore an old backup" flow should degrade gracefully from, not crash on. Told the user plainly: the exact incompatible field in their specific old backup wasn't confirmed (no device console access in this environment), but all three fixes make the failure mode recoverable regardless of which field it turns out to be, and the correct way to move a sermon between two already-synced devices is just Sync on both ends, never a backup-file restore -- restoring an old backup onto an already-syncing device is what caused this, and doing it again would reproduce it.
- **2026-07-21 (root cause confirmed: RLS id collision, and it wasn't only the restored sermon)** — Follow-up the same day. The user's *actual* original complaint -- a sermon written on PC a while back never showing up on the iPad, before the backup-restore workaround made things worse -- turned out to be the same underlying bug, not something separate: had them open the browser console on PC (easy there, no Mac needed, unlike iPad Safari) and got the real error verbatim: `code: '42501', message: 'new row violates row-level security policy (USING expression) for table "sermons"'` on a sermon with id `mr9k00kda83hwz` -- the exact shape of the pre-`crypto.randomUUID()` id fallback (`Date.now().toString(36) + Math.random().toString(36).slice(2,8)`, low-entropy, from before a secure context was guaranteed). 42501 on an upsert means a row with this exact id already exists under a *different* `user_id` -- almost certainly a leftover from earlier testing under a different signed-in account during this app's own development -- and Postgres's RLS is correctly refusing to let this account overwrite a row it doesn't own. Not a bug in the rejection; there was just no recovery path for hitting it. Fixed in `syncOneTable`'s push loop: on `upErr.code === '42501'` for a record that has never itself synced (so there's no remote counterpart of its own to reconcile with), give it a fresh `uid()` and carry along any practice notes keyed to the old sermon id -- the next pass inserts it as a new row under the current account instead of retrying the same doomed update forever. Verified with a Playwright test reproducing the exact error code and id shape from the user's console end to end (id reassigned, practice note follows it, next pass succeeds), plus a re-run of the previous entry's poison-record test confirming the two recovery paths (generic permanent failure vs. this specific RLS collision) don't interfere with each other. Flagged honestly to the user: if the *same* old sermon independently exists in local storage on **both** devices under this identical weak id (plausible if it was ever moved between them via manual backup export/import before sync existed), each device will now reassign it independently and both copies will land on the server as two separate rows rather than merging into one -- an acceptable, communicated trade-off (a duplicate they can delete) over the previous outcome (permanently broken sync).
- **2026-07-21/22 (user punch list, first four items)** — User handed over an 11-item punch list; added verbatim to a new Backlog section (see above) and worked through the four fastest/safest first. (1) `#fontPop` (Present's font-size popup) was `position:absolute` inside `#presentTop`, which is `overflow-x:auto` for its own horizontal-scroll fallback -- a non-`visible` `overflow-x` implicitly makes `overflow-y` non-visible too, clipping the popup's bottom edge since it extends below the bar's own height. Fixed with `position:fixed` (escapes the ancestor's overflow entirely) plus `calc(52px + env(safe-area-inset-top))` to preserve the same visual offset now that the reference frame changed from the bar to the viewport. (2)+(3) The brand mark had two PNG variants already sitting in `icons/` unused as a pair -- `brandbar.png` (dark green, correct on light backgrounds) and `presentbrand.png` (cream, correct on dark backgrounds) -- but Library was hardcoded to always show the former and Present always the latter, so each was right in one theme and wrong in the other. Wired both `<img>`s into the app's existing `data-light`/`data-dark` swap convention (added a shared `.brand-ic` class to the existing `.nav-ic, .size-ic, .mono-ic` swap selector) instead of commissioning new art -- the correct pair already existed, just cross-wired to the wrong single location each. (4) Removed the offline KJV lookup/insert feature entirely per the user's call that the translation doesn't fit the app's premium positioning: the "Insert KJV" button, its click handler, the whole `KJV_BOOKS`/`KJV_BOOK_ALIASES`/`parseScriptureRef`/`loadKjvData`/`formatVerseHtml` block, the 4.1MB `data/kjv.json` file, and its `sw.js` precache entry. Kept `BIBLE_BOOKS`/`BOOK_PATTERN`/`SCRIPTURE_RE` and the "Look up" button (now the only button on a scripture item) -- those are a separate, translation-agnostic scripture-reference regex shared with the ProPresenter RTF export, not part of the KJV feature -- and stripped the hardcoded `&t=KJV` off the Blue Letter Bible search URL so "Look up" no longer forces a specific translation externally either. Verified with Playwright: the scripture item row now renders only "Look up", that URL no longer contains `t=KJV`, and the ProPresenter export (the one thing sharing code with the removed feature) still produces a correct `.rtf` download -- plus a full re-run of the existing regression suite (CSP/export, RevenueCat-web-gate, both sync-engine tests) with no new failures.
- **2026-07-22 (icon audit -- item 5 of the punch list)** — Surveyed every icon reference in `index.html` and every file in `icons/` (cross-checked against `sw.js`'s precache list) rather than guessing. Most of the app is already fully covered: all six bottom-nav tabs but one, the whole editor formatting toolbar (bullet/numbered list, indent/outdent, drag-to-reorder, section up/down/delete), the font-size/dark-mode toggles, and both brand-mark variants all have dedicated hand-drawn light/dark PNG pairs. Three real gaps found, one already flagged in an earlier pass (2026-07-21 homogeneity entry, not re-litigated here) and two genuinely new: (1) **Practice tab's bottom-nav icon** is still a generic geometric stopwatch SVG, visibly thinner/more generic than its five siblings -- previously flagged, still open, still needs real custom art. (2) **The Settings gear icon** (top toolbar) and the **Sync Now icon** (Settings row) are both generic inline Feather-style SVGs, not part of the hand-drawn icon family the rest of the chrome uses -- Sync Now was already upgraded once from a raw Unicode "↻" glyph to this SVG (2026-07-21 homogeneity entry) but that was a technical-correctness fix, not a visual match; neither has ever had a custom-art pass. (3) **Orphaned custom assets**: `icons/backupexportbtn.png`/`backupimportbtn.png` (+ dark variants) exist on disk and are precached by `sw.js`, but nothing in `index.html` actually references them -- the Settings "Backup" and "Restore from Backup" rows render as plain text with a `›` chevron, no icon at all. Likely commissioned for those two rows and never wired in, or wired in once and later replaced by the plain-row Settings redesign (2026-07-20) without removing the now-unused assets. Left as a decision for the user rather than a call to make unilaterally: either wire the two existing PNGs into those rows (free, they already exist and match the icon family) or remove them as dead weight if a text-only Settings row is preferred. No code changes from this item alone -- it's a research/inventory task, its deliverable is this list.
- **2026-07-22 (versioned schema migration -- item 2 of the punch list)** — Replaced ad hoc defensive coercion with a real, extensible migration architecture, motivated directly by the same-day RLS-id-collision incident: the app already had a `schemaVersion` field and one migration's worth of logic (`normalizeItem`, collapsing old bullet/run/para item types and a numeric indent field onto the current 4-type model), but it ran unconditionally on every item of every sermon on every single load forever, regardless of whether that sermon actually needed it -- fine at today's scale, a real cost once a library holds decades of sermons (ties into the still-open Library-sort backlog item, same underlying "assume decades of data" concern). Introduced `SCHEMA` (`{sermon: {current, migrations}, illustration: {current, migrations}}`) and `migrateToCurrent(kind, record)`, which walks a record forward one version at a time from its stored `schemaVersion` to `SCHEMA[kind].current`, running only the migration steps actually between those two numbers -- a record already current costs one number comparison, and a record from *any* past version (not just the immediately previous one) upgrades correctly in one pass, since a skipped app update can never skip a migration step this way. Renamed the old logic `migrateSermonV1toV2` and registered it as `SCHEMA.sermon.migrations[1]`; `normalizeSermon` now does three separable things in order, on purpose: (a) unconditional shape-safety coercion (non-array `sections`/`items` forced to empty) that runs regardless of declared version, since a corrupt or foreign backup can claim any version while not actually matching it; (b) `migrateToCurrent('sermon', s)`, the actual version-gated step; (c) unconditional rich-text sanitization, which is hygiene, not a migration, and stays cheap/idempotent outside the version gate. `normalizeIllustration` became a one-line `migrateToCurrent` call (its `migrations` map starts empty -- scaffolding for whenever illustrations' shape first changes, not dead code, since the same walker already handles it for free). Templates deliberately excluded from `SCHEMA` -- their shape (name/desc/plain-string sections) has never changed and carries no `schemaVersion` field at all; a comment at the `SCHEMA` declaration says to follow the identical pattern if that ever changes, rather than leaving a same-shaped but unused/misleading entry sitting in the map. Also fixed the two call sites that stamped a freshly-created record's `schemaVersion` to a hardcoded `1` (`newSermonFromTemplate`, the illustration-add flow) to reference `SCHEMA.sermon.current`/`SCHEMA.illustration.current` instead -- a brand-new record made in the current build is definitionally already current, and hardcoding `1` would have quietly re-triggered the v1→v2 migration's item-shape assumptions against content that was never actually in that old shape. Verified with a new Playwright test (`test-schema-migration.js`): a genuinely old-shape sermon (no `schemaVersion` field, `bullet`/`run`/`para` items, a numeric `indent`, a `fromIllustration` flag) migrates correctly on load -- ends up at `schemaVersion: 2`, all four item types collapsed to the current model, `indent`/`fromIllustration` stripped -- while a sermon already stamped `schemaVersion: 2` passes through untouched in content (proving the version gate actually skips the migration step, not just skips re-declaring the version); a brand-new sermon created via "+ New" stamps `schemaVersion: 2`, not `1`. Re-ran the full existing regression suite (CSP/export, RevenueCat-web-gate, web-gate, both sync-engine tests, the poison-record and RLS-collision recovery tests, KJV removal, brand-icon theming, font-popup clipping) with no new failures; one pre-existing `Cannot read properties of undefined (reading 'slice')` page error in `test-sync2.js` was confirmed (via `git stash`) to reproduce identically before this change, so it's unrelated noise, not a regression.
- **2026-07-22 (conflict false-positives fixed -- item 4 of the backlog, first pass)** — Asked the user what specifically was frustrating about sync conflicts; answer: they fire too often. Root cause, confirmed by re-reading `syncOneTable`'s pull loop: a conflict copy is created purely from timestamps -- `local.updatedAt` and the remote row's `updated_at` both newer than the record's last-synced watermark -- with zero check of whether the two versions' actual content differs. That's the textbook false positive: the same edit reaching the server from two devices, or a save that only ever touched a bookkeeping field, moves both timestamps past the watermark with nothing to actually reconcile, yet got treated exactly like a real simultaneous edit on two devices. Added `deepEqualIgnoring(a, b, ignoreKeys)` (canonicalizes both objects with sorted keys before a `JSON.stringify` comparison, so key-insertion-order differences between a locally-edited object and one freshly rebuilt by `fromRemote()` can't cause a false mismatch) and now check it before cloning anything: if the incoming and local content are identical once `id`/`updatedAt`/`schemaVersion`/`conflictOf` are ignored, the newer timestamp just wins with no duplicate card at all. Hit one real snag proving the test was worth writing rather than eyeballing the diff: a local record that's never round-tripped through `fromRemote` simply omits the `deleted` key instead of carrying an explicit `false` (`fromRemote` always sets one) -- coerced both sides with `!!` before comparing specifically for that field, rather than weakening the general-purpose equality check for everything else. Also fixed a real, separate, pre-existing bug noticed while in this exact code: the conflict-copy's "(synced copy)" suffix was hardcoded onto `.title`, which templates don't have (they use `.name`) -- their conflict copies got the "Synced copy" tag badge but no actual name change. Now picks whichever field the record actually has. Verified with a new Playwright test covering both halves in one pass: a sermon where local and remote both "changed" per timestamp but ended up with byte-identical content creates zero duplicate cards; a sermon with genuinely different content on each side still creates the clone with a correctly-suffixed name and working Keep This/Keep Both, unchanged from before. Full existing regression suite re-run clean. **Not done in this pass, left open per the Backlog item above**: making a genuine conflict's *original* card show that it was overwritten (today it still changes silently), and making Keep This/Keep Both self-explanatory without opening both cards to compare -- that's a UI/legibility problem, separate from this pass's false-positive-rate problem, and needs its own design pass.
- **2026-07-22 (sign-in persistence: leading theory + first mitigation, item 1 of the backlog)** — Asked the user for more detail on the sign-in-doesn't-survive-a-refresh report; their answer (happens even on a light/new browser profile) ruled out the initial theory that this was localStorage quota pressure from the sermon/illustration data already living there. Re-read the app's own service-worker wiring with that ruled out and found a genuinely plausible mechanism specific to this app: `controllerchange` (fired whenever a new `sw.js` version takes control) triggers an unconditional, unguarded `window.location.reload()` -- added deliberately so a tab left open doesn't keep running stale code, but with no awareness of what else might be happening on the page at that exact moment. A sign-in via Google OAuth or an email magic link redirects back to the app with the session encoded in the URL (`#access_token=...` or `?code=...`), and Supabase's client detects and persists that asynchronously on load (`detectSessionInUrl`) -- if a new service worker happens to take control in that same window (extremely plausible during active development, where a new version can ship at any time), the resulting reload could fire before the session finishes being written to storage, stranding it as memory-only or losing it outright. Not confirmed with a real device -- this is the best mechanism found by reading the code, not a reproduced bug. Shipped two things regardless, both safe on their own even if this isn't the exact cause: (1) the `controllerchange` handler now checks whether the current URL still looks like an in-progress auth redirect (`access_token=`, `refresh_token=`, `type=magiclink/recovery/signup`, or a bare `code=` query param) and defers the reload in 400ms steps until it doesn't, with an 8-second deadline that forces the reload through regardless as a backstop against this check ever being wrong about what a redirect looks like; (2) a console diagnostic (`[auth] stored session on load: present/absent`) logs on every load whether a Supabase auth-token key actually exists in localStorage, with no debug flag gating it, specifically so a future recurrence can be confirmed with real evidence instead of another guess -- same "get the actual console output" methodology that cracked the RLS-collision bug earlier this project. Verified with a new Playwright test: dispatching a real `controllerchange` event while the URL carries a fake `#access_token=` correctly defers the reload (confirmed via a `load`-event counter that survives across the eventual reload) until the URL is cleared, at which point the deferred reload fires on its next poll; separately confirmed the diagnostic correctly logs "absent" with no stored session and "present" when a `sb-*-auth-token` key exists. Told the user plainly: if this happens again, the console line is the way to actually settle it.
- **2026-07-22 (Practice notes: full text instead of a 140-char excerpt -- item 5 of the backlog, resolved)** — Asked what the original "tap-to-highlight a text range" request was really after; the user confirmed item-level annotation (the existing model) is fine, and true sub-item character-range highlighting isn't worth its own real complexity. The actual complaint, once asked directly: for a longer paragraph, the note popup's excerpt (`#practiceNoteExcerpt`) was truncated to 140 characters, so it was hard to tell which specific text a note was actually about once the item ran past that length -- especially reviewing a note later, well after writing it. Fix: `openPracticeNoteModal` now renders the item's *full* plain text, not a `.slice(0,140)` snippet; `.practice-note-excerpt` got its own `max-height:120px; overflow-y:auto` so a genuinely long paragraph scrolls within its own box instead of pushing the textarea and Save button down and off the popup's visible area. The Notes drawer's list view (`renderPracticeNotesList`, a summary of every note for the sermon) deliberately still uses the original 140-char stored excerpt -- that's a scannable list of potentially many notes, not the single-note editing context the complaint was about, and showing full paragraphs there would hurt scannability rather than help it. Verified with Playwright at a phone-width (390px) viewport with a 12-sentence/~1500-character seeded paragraph: the popup shows the item's last sentence (proving no truncation), the excerpt box itself stays capped at the intended height, and the Save button remains fully on-screen and reachable without first having to scroll past the whole paragraph.
- **2026-07-22 (Library: year filter for decade-scale libraries -- item "sort options" resolved)** — User's answer: at decade-scale, what matters most is narrowing down first (by year or series), then sorting within that smaller set. The app already had most of this: a series chip filter (`#seriesFilter`/`activeSeriesFilter`), a sort toggle (Recent / By Date, the latter grouping into Upcoming/Past), and a text search -- all of which already compose (search + series filter + sort all already applied together in `renderLibrary`). The actual gap: no way to narrow by *year*, which matters for exactly the cases series can't reach -- an undated-relative-to-series one-off sermon, or a series that itself spans multiple years. Added a `#yearFilter` `<select>` (deliberately a dropdown, not another chip row, alongside the existing series chips -- a chip row scales fine for a modest number of distinct series but stops being scannable once it has to hold one chip per year of ministry) populated from the distinct years actually present across non-deleted sermons, newest first, hidden entirely when there's one year or fewer (matching the series filter's existing behavior of hiding when there's nothing to filter by). `activeYearFilter` composes with the existing `activeSeriesFilter` and search query with AND logic, and the existing empty-state message now also covers "filtered to zero by year." Verified with Playwright across a 4-sermon set spanning 2010/2018/2018/2024: the year dropdown lists years newest-first, filtering to 2018 alone or combined with a series filter narrows correctly, a year+series combination with zero matches shows the existing empty-state message, and resetting both filters restores the full list; separately confirmed the dropdown hides itself (rather than showing a single useless option) when only one year exists in the data, and that the new filter row doesn't overflow a 390px phone viewport.
- **2026-07-22 (Plan/calendar: search box, decided and implemented)** — User resolved the open design question directly: a search box, not a drag-reorderable list. Added `#planSearchInput` above the calendar grid (outside the `.scroll` area, same convention as Library's own search bar) that matches against sermon title and series as you type. Deliberately doesn't replace the calendar with a plain results list -- a sermon's scheduled date is meaningful information worth keeping visible, and clicking prev/next through potentially hundreds of months to find one sermon from years ago is the actual thing that doesn't scale, not the calendar UI itself. So a match click instead: jumps `planMonth` to that sermon's month and re-renders the grid, briefly highlights the exact day cell (a 3-second gold outline, `.plan-day.search-match`, added via a new `data-date` attribute per cell so a click handler can find the right one without re-deriving it from scratch) and scrolls it into view, then clears the search box and closes the results dropdown -- landing the user looking at the real calendar cell in context, not just a search-results list standing in for it. A genuinely empty result set shows an explicit "no series or sermon titles match" message rather than silently showing nothing. Verified with Playwright across sermons spanning 2015/2018/2026: typing narrows to matching results (by title or series), an unmatched query shows the empty-state message, and clicking a 2015 result correctly flips the month label to December 2015, highlights the `2015-12-20` cell specifically, and clears/closes the search UI -- plus a phone-width (390px) layout check confirming the new search row and results dropdown don't overflow.
- **2026-07-22/23 (RLS-collision auto-recovery: the real fix, item 4's "still getting sync errors" report)** — The user was reviewing last night's changes one by one; on item 4 (the conflict false-positive fix) they reported still seeing the exact same `mr9k00kda83hwz` / 42501 error from before, even after the auto-recovery fix had shipped. Confirmed via GitHub first (not assumed): the merged `main` branch's `sw.js` was already at the build that shipped the fix, and the console screenshot they sent opened with `[auth] stored session on load: present` -- a line that only exists in code from *after* the recovery fix -- so this wasn't a stale-deploy or stale-cache repeat of the earlier false alarm. Re-read the recovery code with that ruled out and found the real bug: the auto-reassignment only fired when `!syncedAt(cfg.table, r.id)` (the record had never itself synced locally), on the theory that a record with a local syncedAt watermark must have synced successfully before and so couldn't plausibly collide. That reasoning doesn't hold: `shema_sync_meta_v1` is a single localStorage key with no per-account scoping, so a device that ever synced this exact id under a *different* signed-in account (very plausible mid-development, testing with more than one account on one browser) keeps a stale, truthy watermark for that id forever -- permanently blocking the one recovery path that exists to fix exactly this. Fixed by reassigning on *any* 42501, unconditionally, since there is no scenario where retrying the same id later succeeds without someone else deleting the conflicting row server-side -- the per-account caution was actively causing the permanent failure it was meant to guard against. Also now deletes the stale watermark entry for the old id when reassigning, closing the actual mechanism that produced this bug rather than just working around its symptom. Verified with a new Playwright test that seeds exactly the failure condition (a local syncedAt watermark for the id that predates its current updatedAt, simulating a prior sync under a different account) and confirms the id is still reassigned, the stale watermark is cleaned up, and the reassigned sermon successfully lands on the server on the next pass -- plus the original id-collision test and the rest of the sync regression suite re-run clean.
- **2026-07-23 (sign-in persistence, user-confirmed resolved)** — During the item-by-item review of the 2026-07-22 session's changes, the user confirmed the sign-in-persistence mitigation (auto-reload guard + diagnostic) actually fixed the problem in real use. The exact root cause was never independently proven beyond the code-level theory (the SW-update auto-reload racing an in-progress auth redirect) -- but the fix holds up in practice, so this is closed rather than left open pending further diagnosis.
